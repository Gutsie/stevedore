# Required
STEVEDORE_SECRET=change-me-to-a-long-random-string
STEVEDORE_COMPOSE_PROJECT_DIR=/project

# Optional
# STEVEDORE_COMPOSE_CLI=docker compose
# STEVEDORE_COMPOSE_CLI=docker-compose
# STEVEDORE_COMPOSE_FILES=docker-compose.yml
# STEVEDORE_COMPOSE_FILES=compose.yaml,compose.override.yaml
# STEVEDORE_DEPLOY_COMMAND=docker compose pull && docker compose up -d --remove-orphans
# STEVEDORE_LISTEN_HOST=0.0.0.0
# STEVEDORE_PORT=8080
# STEVEDORE_REQUEST_BODY_MAX_BYTES=65536
# STEVEDORE_DEPLOY_TIMEOUT_MS=600000
